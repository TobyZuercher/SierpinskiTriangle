public void makeSierpinskiCube(float x, float y, float len, PShape s) {
  s = createShape(GROUP);
  s.beginShape();
  sierpinskiCube(x, y, len);
  s.endShape();
}

public void sierpinskiCube(float x, float y, float len) {
  
}
